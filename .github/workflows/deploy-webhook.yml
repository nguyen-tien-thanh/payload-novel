name: Deploy Webhook

on:
  push:
    branches:
      - develop
  workflow_dispatch:

jobs:
  trigger-deploy:
    runs-on: ubuntu-latest
    environment: n8n

    steps:
      - name: Trigger n8n deploy webhook
        env:
          WEBHOOK_URL: https://n8n.tiralix.com/webhook/deploy
        run: |
          curl --fail --show-error --silent \
            --request POST \
            --header "Content-Type: application/json" \
            --data @- \
            "$WEBHOOK_URL" <<'EOF'
          {
            "app": "payload",
            "repository": "${{ github.repository }}",
            "ref": "${{ github.ref }}",
            "sha": "${{ github.sha }}",
            "actor": "${{ github.actor }}",
            "event_name": "${{ github.event_name }}"
          }
          EOF
